{
  "name": "@task/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A sample localization project. Run the CLI here, against real files.",
  "scripts": {
    "cli": "lng"
  },
  "dependencies": {
    "@task/cli": "workspace:*"
  }
}
